backend default {
        .host = "localhost";
        .port = "8080";
}

sub vcl_fetch {
        # force minimum ttl of 120 seconds
        if (beresp.ttl < 120s) {
                set beresp.ttl = 120s;
        }
}
